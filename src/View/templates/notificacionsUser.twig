{% extends "base.twig" %}
{% block title %} Comentaris {% endblock %}
{% block head %}
    {{ parent() }}
    <link rel="stylesheet" href={{ asset('/userComments.css', 'css') }}>
{% endblock %}
{% block content %}
    <div class="container">
        <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css" rel="stylesheet" integrity="sha384-T8Gy5hrqNKT+hzMclPo118YTQO6cYprQmhrYwIiQ/3axmI1hQomh7Ud2hPOy8SP1" crossorigin="anonymous">
        <div class="row">
            <!-- Contenedor Principal -->
            <div class="comments-container ">
                <h1>Notificacions de Comentario:</h1>
                <ul id="comments-list" class="comments-list">
                    {% for n in notificacions %}
                       {% if n.type == 1 %}
                        <li>
                            <div class="comment-main-level">
                                <!-- Contenedor del Comentario -->
                                <div class="comment-box">
                                    <div class="comment-head">
                                        <a> {{ n.nom_usuari }}  </a>
                                        <a href="#" ><i class="icon-ok-sign"></i> icon-ok-sign </a>
                                    </div>
                                    <div class="comment-content">
                                        <p> Ha comentado en  {{ n.titol }} </p>
                                    </div>
                                </div>
                            </div>
                            <!-- Respuestas de los comentarios -->
                        </li>
                       {% endif %}
                    {% endfor %}
                </ul>
            </div>

            <div class="comments-container ">
                <h1>Notificacions de Comentario:</h1>
                <ul id="comments-list" class="comments-list">
                    {% for n in notificacions %}
                        {% if n.type == 2 %}
                            <li>
                                <div class="comment-main-level">
                                    <!-- Contenedor del Comentario -->
                                    <div class="comment-box">
                                        <div class="comment-head">
                                            <a> {{ n.nom_usuari }}  </a>
                                            <a href="#" ><i class="icon-ok-sign"></i> icon-ok-sign </a>
                                        </div>
                                        <div class="comment-content">
                                            <p> Le ha dado like a la siguiente imagen  {{ n.titol }} </p>
                                        </div>
                                    </div>
                                </div>
                                <!-- Respuestas de los comentarios -->
                            </li>
                        {% endif %}
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>



{% endblock %}